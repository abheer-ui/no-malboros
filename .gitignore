# Dependencies
node_modules/

# The previous version of the app. Kept locally for reference only —
# it must never reach the repo or a public deploy: it is dead code, and it
# ships a Supabase auth UI that no longer has a backend behind it.
legacy/

# OS / editor noise
.DS_Store
*.swp
.idea/
.vscode/

# macOS alias files for an unrelated editor — they are files, not folders
/Antigravity
/Antigravity IDE
/untitled folder

# Local tooling and personal skill files — not part of the app
.claude/
